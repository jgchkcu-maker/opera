name: Apply builder summary fix

on:
  push:
    branches: ["fix-builder-summary-panel"]

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix-builder-summary-panel
      - name: Smoke
        run: echo "builder summary patch runner is valid"
